#include"bits/stdc++.h"
using namespace std;

int main(){
    int n, m;
    cin >> n >> m;
    int c[n];
    for(int i = 0; i < n; i++) c[i] = 0;
    for(int i = 0; i < m; i++){
        cin >> c[i] >> c[i+m];
    }
    for(int i = 0; i < n; i++){
        if(c[i] )
    }
}